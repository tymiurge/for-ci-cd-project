import { notToUse } from './warnings'
import { strIsNumber } from './validation'
import { chunkArray } from './arrays'

const warnings = {
    notToUse
}

const validation = {
    strIsNumber
}

const arrays = {
    chunkArray
}

export {
    warnings,
    validation,
    arrays
}
